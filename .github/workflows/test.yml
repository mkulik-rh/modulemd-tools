---
name: Tests

on:
  workflow_dispatch:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    concurrency: test-env
    environment: test-env
    container:
      image: ghcr.io/mkulik-rh/docker-builder:latest
      credentials:
        username: mkulik-rh
        password: ${{ secrets.GHCR_PAT }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run tests
        run: |
          tox -vvv -e py310,flake8
